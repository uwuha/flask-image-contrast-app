image: Visual Studio 2022

stack: python 3.8

environment:
  matrix:
    - PYTHON: "C:\\Python38"

install:
  - pip install -r requirements.txt

build: off

test_script:
  - python -m pytest tests/ -v

after_test:
  - python app.py &
  - sleep 10
  - curl http://localhost:5000 || echo "Server started successfully"